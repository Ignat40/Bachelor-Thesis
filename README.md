## Repositories

- Django backend/web dashboard: https://github.com/Ignat40/Bachelor-Thesis.git
- Unity client: https://github.com/Rototype0/Phonemy-Client.git

## Running the Django Web Dashboard

1. Clone the Django repository.
2. Create and activate a Python virtual environment.
3. Install dependencies:

```bash
pip install -r requirements.txt
```

4. Configure the required environment variables/database settings.
5. Run migrations:

```bash
python manage.py migrate
```

6. Start the server:

```bash
python manage.py runserver
```

7. Open:

```text
http://localhost:8000
```

